<span id="notice"><%= notice %></span>
<div class="pagehead">
    <div class="container">
        <div class="titlehead">Cots</div>
    </div>
</div>

<div class="album py-5 bg-light">
    <div class="container">
      <div class="row">
        <% @cots.each do |cot| %>
        <div class="col-md-4">
          <div class="card mb-4 shadow-sm">
            <%= image_tag cot.image_url.to_s, class: "card-img-top", style: 'height:225px;width:100%;display: block;' %>
            <div class="card-body">
              <p class="card-text"><%= cot.cottitle %></p>
            </div>
          </div>
        </div>
        <% end %>
      </div>
    </div>
</div>